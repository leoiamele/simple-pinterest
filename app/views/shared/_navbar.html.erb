 <nav class="navbar navbar-dark bg-dark justify-content-between">
      <a class="navbar-brand text-light">Pinterest</a>
      <form class="form-inline ml-auto">
        <input class="form-control mr-sm-2" name="q" type="search" placeholder="Search" aria-label="Search">
        <button class="btn btn-outline-success my-2 my-sm-0" type="submit">Search</button>
      </form>
      <% if user_signed_in? %>
        <%= link_to "Sing Out", destroy_user_session_path, method: :delete, class: "ml-2 btn btn-outline-primary"%>
      <% else %>
        <%= link_to "Sing In", user_session_path, method: :post, class: "ml-2 btn btn-outline-primary"%>
      <% end %>
    </nav>
